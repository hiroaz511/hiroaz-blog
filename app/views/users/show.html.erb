 <div>
      <!-- 以下で、ユーザー名を出力-->
      <h2><%= @user.name %></h2>
      <!-- 以下で、メールアドレスを出力-->
      <p><%= @user.email %></p>
      <%= link_to("Edit", "/users/#{@user.id}/edit") %>
      <%= link_to("ユーザー一覧へ", "/users/index")%>
    </div>