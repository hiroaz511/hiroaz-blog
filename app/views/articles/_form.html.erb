<%= form_with model: @article, local: true do |form| %>
  <% if @article.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@article.errors.count, "error") %> prohibited
      this article from being saved:</h2>
    <ul>
    <% @article.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <div class="text-center">
    <p class="mt-4">
      <%= form.label :タイトル %><br>
      <input class="form-control z-depth-1 col-xs-12 col-md-8 col-lg-7 mx-auto" name="title"></input>
    </p>

    <p class="mt-5">
      <%= form.label :本文 %><br>
      <textarea class="form-control z-depth-1 col-xs-12 col-md-8 col-lg-7 mx-auto" rows="3" placeholder="自由に書いてください" name="text"></textarea>
    </p>

    <div class="mx-auto text-center mt-4 mb-4">
      <%= form.submit :投稿, class: "btn btn-primary" %>
    </div>
  </div>
<% end %>